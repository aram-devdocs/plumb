# Agent runs

Scratch space for long-running agent tasks (one subdirectory per run,
usually named `issue-<n>-<slug>/` or `pr-<n>-<slug>/`).

Everything under `runs/` except this README is gitignored — see
`.gitignore`.
